
@using Vfi.Ui.Mvc.Vfi.Areas.Inv.Models
@model Vfi.Ui.Mvc.Vfi.Areas.Inv.Models.ImportSX1Model
@{
    ViewBag.Title = "Phiếu Phát Nguyên Liệu";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@section Scripts{
   
    <script type="text/javascript">
        function grid_OnDataBinding(e) {
            
            var factory = $("#Factory").data('tComboBox').value();
            e.data = $.extend(e.data, {
                factory: factory
            });
            
        }

        function grid_OnSave(e) {
            var $materialCode = $(e.cell).find('#MaterialInventoryCode');
            if ($materialCode.length > 0) {
                var materialCode = $materialCode.data("tComboBox"),
                    materialCodeSelectItem = materialCode.selectedIndex > -1 ? materialCode.data[materialCode.selectedIndex] : null;

                if (materialCodeSelectItem) {
                    e.values["MaterialInventoryCode"] = materialCodeSelectItem.Text;
                    e.values["MaterialInventoryId"] = materialCodeSelectItem.Value;
                    $.ajax({
                        type: 'post',
                        data: { machineId: e.dataItem.MachineId, materialInventoryId: materialCodeSelectItem.Value },
                        dataType: 'json',
                        url: '@Url.Action("GetAssignMaterial", "Material",new{area=""})',
                        timeout: 20000,
                        success: function (data) {
                            if (data == '9') {
                                alert("Lỗi! Không tìm thấy tồn nguyên liệu");
                            } else if (data == '0') {
                                alert("Lỗi!");
                            } else {
                                e.cell.parentElement.childNodes[2].textContent = data[0];
                                e.cell.parentElement.childNodes[3].textContent = data[1];
                            }
                        }
                     });
                }
            }
           
            //tong number1
            var number1 = $(e.cell).find('#MaterialUse1').val();
            if (!isNaN(parseInt(number1))) {
                var sum = 0.0;
                var spans = $("span[name='MaterialUse1']");
                number1 = parseFloat(number1);
                for (var i = 0; i < spans.length; i++) {
                    var innerText = spans[i].innerText;
                    innerText = innerText.replace(/,/g, '');
                    sum = sum + parseFloat(innerText);
                }
                sum = sum + number1;
                $("#TotalMaterialUse1").text(sum);
                if (number1 > e.dataItem.DiffMaterial) {
                    e.cell.bgColor = "yellow";
                } else {
                    e.cell.bgColor = "";
                }
            }

        }

        function grid_OnRowDataBound(e) {
            //if (e.dataItem.IsLimit) {
            //    e.row.style.backgroundColor = "#FA5858";
            //}
        }
        function grid_onSubmitChanges(e) {
            var importDate = $("#ImportDate").data("tDatePicker").value();
            if (importDate === null)
                importDate = "";
            else
                importDate = $.telerik.formatString('{0:dd/MM/yyyy}', importDate);

            var caNumber = $("#CaNumber").data("tDropDownList").value();
            var caName = $("#CaName").data("tDropDownList").value();
            
           // console.log(e.values);
            e.values = $.extend(e.values,
                {
                    importDate: importDate,
                    caNumber: caNumber,
                    caName: caName,
                }
            );

            productIds = {};
            productIds[0] = 0;
        }

        function CaNumber_onload() {
            var combobox1 = $('#CaNumber').data('tDropDownList');
            var dataSource =
            [
                { Text: "1", Value: "1" },
                { Text: "2", Value: "2" }
            ];

            combobox1.dataBind(dataSource);
            //combobox1.Text("1");
        }

        function CaName_onload() {
            var combobox2 = $('#CaName').data('tDropDownList');
            var dataSource =
            [
                { Text: "A", Value: "A" },
                { Text: "B", Value: "B" },
                { Text: "C", Value: "C" },
                { Text: "D", Value: "D" }
            ];

            combobox2.dataBind(dataSource);
        }

        function Factory_Onload() {
            var combobox = $('#Factory').data('tComboBox');
            var dataSource =
            [
                { Text: "VF1", Value: "VF1" },
                { Text: "VF2", Value: "VF2" }
            ];

            combobox.dataBind(dataSource);
        }
    </script>
}

    <div class="title">
        @*Tạo Phiếu Phát Nguyên Liệu*@
        @HttpContext.GetGlobalResourceObject("Resource", "AssignMaterials")
    </div>
    <div class="t-info-sha" style="padding-left: 15px; padding-right: 15px; width: 920px;">

        <fieldset>
            <legend>
                @*Thông tin phiếu*@
                @HttpContext.GetGlobalResourceObject("Resource", "TransactionInformation")
            </legend> 
            <div class="grid_7">
               
            </div>
            <div class="grid_4">
                @*Ngày sử dụng sản phẩm*@
                @HttpContext.GetGlobalResourceObject("Resource", "ProductUsingDate")
            </div>
            <div class="grid_4">
                @{
                    Html.Telerik().DatePicker()
                        .Name("ImportDate")
                        .Format("dd/MM/yyyy")
                        .Value(DateTime.Today)
                        .Render();
                }
            </div>
            @*<div class="clear" style ="margin-bottom: 10px"></div>*@
        
            <div class="grid_1">
                @*Ca*@
                @HttpContext.GetGlobalResourceObject("Resource", "Shift"):
            </div>
            <div class="grid_2">
                @{
                    Html.Telerik().DropDownList()
                        .Name("CaNumber")
                        .HtmlAttributes(new {style = "width: 40px"})
                        .DropDownHtmlAttributes(new {style = "width: 40px"})
                        .ClientEvents(events => events.OnLoad("CaNumber_onload"))
                        .Render();
                }
            </div>
            <div class="grid_2">
                @{
                    Html.Telerik().DropDownList()
                        .Name("CaName")
                        .HtmlAttributes(new {style = "width: 40px"})
                        .DropDownHtmlAttributes(new {style = "width: 40px"})
                        .ClientEvents(events => events.OnLoad("CaName_onload"))
                        .Render();
                }
            </div>
            <div class="clear"></div><br />
            <div class="grid_2">
                @*Xưởng:*@ 
                @HttpContext.GetGlobalResourceObject("Resource", "Factory"):
            </div>
            <div class="grid_2">
                @{
                    Html.Telerik().ComboBox()
                        .Name("Factory")
                        .ClientEvents(events => events.OnLoad("Factory_Onload"))
                        .Render();
                }
            </div>
        </fieldset>
    </div>
    <div class="clear"></div>
<div class="cover-div">
    @{
        Html.Telerik().Grid<SmartProductionModel>()
            .Name("gridPurchaseOrderDetailPartial")
            .DataKeys(keys => keys.Add(f => f.SmartId).RouteKey("smartId"))
            .ToolBar(bar => {
                    //bar.Insert().ButtonType(GridButtonType.ImageAndText).Text("Thêm dòng mới");
                    bar.SubmitChanges().ButtonType(Telerik.Web.Mvc.UI.GridButtonType.ImageAndText)
                        .SaveText("Lưu tất cả")
                        .CancelText("Làm mới lại");

                })
            .DataBinding(dataBindings => dataBindings.Ajax()
                                                     .Select("SelectAssignMaterials", "Machine", new { area = "Factory" })
                                                     .Update("UpdateAssignMaterialsProduction", "Transaction")
            )
            .Columns(columns => {
                columns.Bound(f => f.MachineName).Width(100)
                    //.Title("Máy")
                    .Title(@HttpContext.GetGlobalResourceObject("Resource", "Machine").ToString())
                    .ReadOnly(true);
            
                columns.Bound(f => f.ProductCode).Width(150)
                    //.Title("SP")
                    .Title(@HttpContext.GetGlobalResourceObject("Resource", "Product2").ToString())
                    .ReadOnly(true);
                
                columns.Bound(f => f.MaterialInventoryCode).Width(200)
                    //.Title("Nguyên liệu - Lô")
                    .Title(@HttpContext.GetGlobalResourceObject("Resource", "Material").ToString()
                        + " - "
                        +@HttpContext.GetGlobalResourceObject("Resource", "Lot").ToString()
                    )
                    .ReadOnly(true);
                
                columns.Bound(f => f.MaterialInvTotal).Width(75).ReadOnly(true)
                    //.HeaderTemplate("Tồn kho</br>(cây)")
                    .Title(@HttpContext.GetGlobalResourceObject("Resource", "Inventory").ToString()
                        + "</br>("
                        + @HttpContext.GetGlobalResourceObject("Resource", "Bar").ToString()
                        + ")"
                    )
                    .Format("{0:n2}")
                    .HtmlAttributes(new { style = "text-align: right;border-left:1px solid black;" });
                
                columns.Bound(f => f.MaterialInvOnMachine).Width(70).ReadOnly(true)
                    //.HeaderTemplate("Tồn máy</br>(cây)")
                    .Title(@HttpContext.GetGlobalResourceObject("Resource", "MachineInv").ToString()
                        + "</br>("
                        +@HttpContext.GetGlobalResourceObject("Resource", "Bar").ToString()
                        + ")"
                    )
                    .Format("{0:n2}")
                    .HtmlAttributes(new { style = "text-align: right;" });
                
                columns.Bound(f => f.DiffMaterial).Width(70).ReadOnly(true)
                    //.HeaderTemplate("Tồn máy</br>TT (cây)")
                    .Title(@HttpContext.GetGlobalResourceObject("Resource", "MachineInv").ToString()
                        + "</br>"
                        + @HttpContext.GetGlobalResourceObject("Resource", "Actual2").ToString()
                        + " ("
                        + @HttpContext.GetGlobalResourceObject("Resource", "Bar").ToString()
                        + ")"
                    )
                    .Format("{0:n2}")
                    .HtmlAttributes(new { style = "text-align: right;" });
                
                columns.Bound(f => f.MaterialUse1).Width(70)
                    //.HeaderTemplate("SL Phát</br>(cây)")
                    .Title(@HttpContext.GetGlobalResourceObject("Resource", "Quantity2").ToString()
                        + " "
                        + @HttpContext.GetGlobalResourceObject("Resource", "Assign").ToString()
                        + "</br>("
                        + @HttpContext.GetGlobalResourceObject("Resource", "Bar").ToString()
                        + ")"
                    )
                    .HeaderHtmlAttributes(new { @class = "edit-column" })
                    .ClientTemplate("<span name='MaterialUse1'><#= MaterialUse1 == null ? 0 : $.telerik.formatString('{0:n2}', MaterialUse1) #></span>")
                    .FooterTemplate("<span id='TotalMaterialUse1'></span>")
                    .HtmlAttributes(new { style = "text-align: right;border-right:1px solid black;" });

                columns.Bound(f => f.Note)
                    //.Title("Ghi Chú")
                    .Title(@HttpContext.GetGlobalResourceObject("Resource", "Note").ToString());
             
                columns.Bound(f => f.TotalMaterialInv).Width(90).ReadOnly(true)
                    //.HeaderTemplate("Σ tồn</br>NL(kg)")
                    .Title("Σ "
                        + @HttpContext.GetGlobalResourceObject("Resource", "Inv").ToString()
                        + "</br>"
                        + @HttpContext.GetGlobalResourceObject("Resource", "Material2").ToString()
                        + "(kg)"    
                    )
                    .Format("{0:n2}")
                    .HtmlAttributes(new { style = "text-align: right;border-left:1px solid black;" });
                
                columns.Bound(f => f.MaterialLimitQuantity).Width(90).ReadOnly(true)
                    //.HeaderTemplate("Hạn mức</br>NL(kg)")
                    .Title(@HttpContext.GetGlobalResourceObject("Resource", "Limit").ToString()
                        + "</br>"
                        + @HttpContext.GetGlobalResourceObject("Resource", "Material2").ToString()
                        + "(kg)"
                    )
                    .Format("{0:n2}")
                    .HtmlAttributes(new { style = "text-align: right;" });
                
                columns.Bound(f => f.ProductLimitQuantity).Width(90).ReadOnly(true)
                    //.HeaderTemplate("Hạn mức</br>SP(pcs)")
                    .Title(@HttpContext.GetGlobalResourceObject("Resource", "Limit").ToString()
                        + "</br>"
                        + @HttpContext.GetGlobalResourceObject("Resource", "Product2").ToString()
                        + "(pcs)"
                    )
                    .Format("{0:n0}")
                    .HtmlAttributes(new { style = "text-align: right;" });
                
                columns.Bound(f => f.MaterialUsedQuantity).Width(90).ReadOnly(true)
                    //.HeaderTemplate("NL</br>đã SD")
                    .Title(@HttpContext.GetGlobalResourceObject("Resource", "Material2").ToString()
                        + "</br>"
                        + @HttpContext.GetGlobalResourceObject("Resource", "Used2").ToString()
                    )
                    .Format("{0:n2}")
                    .HtmlAttributes(new { style = "text-align: right;" });
                
                columns.Bound(f => f.MaterialLimitLeft).Width(09).ReadOnly(true)
                    //.HeaderTemplate("HM NL</br>còn lại")
                    .Title(@HttpContext.GetGlobalResourceObject("Resource", "Limit2").ToString()
                        + " "
                        + @HttpContext.GetGlobalResourceObject("Resource", "Material2").ToString()
                        + "</br>"
                        + @HttpContext.GetGlobalResourceObject("Resource", "LeftOver").ToString()
                    )
                    .Format("{0:n2}")
                    .HtmlAttributes(new { style = "text-align: right;" });
                
                columns.Bound(f => f.ProductionQuantity).Width(90).ReadOnly(true)
                    //.HeaderTemplate("SP</br>đã SX")
                    .Title(@HttpContext.GetGlobalResourceObject("Resource", "Product2").ToString()
                        + "</br>"
                        + @HttpContext.GetGlobalResourceObject("Resource", "ProductionCompleted2").ToString()
                    )
                    .Format("{0:n0}")
                    .HtmlAttributes(new { style = "text-align: right;" });
            })
            .ClientEvents(events => events
                                        .OnDataBinding("grid_OnDataBinding")
                                        .OnSave("grid_OnSave")
                                        .OnSubmitChanges("grid_onSubmitChanges")
                                        .OnError("grid_OnError")
                                        .OnRowDataBound("grid_OnRowDataBound")
            )
            .Editable(editing => editing.Mode(Telerik.Web.Mvc.UI.GridEditMode.InCell))
            .KeyboardNavigation(keys => keys.EditOnTab(true))
            .Resizable(resizing => resizing.Columns(true))
            .Scrollable(f => f.Height(510))
            // .Footer(false)
            .Render();
    }
</div>
<div class="clear"></div><br />