@using Vfi.Ui.Mvc.Vfi.Models
@{
    ViewBag.Title = "Home Page";
}


@if (!Request.IsAuthenticated)
{
    //Html.RenderPartial("WindowLogOn");
    Html.RenderPartial("_WindowLogOn");
    
}

@*    <div class="clear"></div><br/>
    <div class="cover-div">
        @{
            Html.Telerik().Grid<SystemMessage>()
                .Name("gridPurchaseOrderDetailPartial")
                //.DataKeys(keys => keys.Add(f => f.MessageType).RouteKey("messageType"))
                .DataBinding(dataBindings => dataBindings.Ajax()
                                                         .Select("SelectMessage", "Home")
                )
                .Columns(columns =>
                {
                    columns.Bound(f => f.MessageType).Width(50).Title("Loại");
                    columns.Bound(f => f.Message).Width(50).Title("Tin");
                    columns.Bound(f => f.Number).Width(50).Title("Số lượng");
                    })
                .ClientEvents(events => events.OnError("grid_OnError"))
                .Resizable(resizing => resizing.Columns(true))
                .Render();
        }
    </div>
    <div class="clear"></div><br/>*@

