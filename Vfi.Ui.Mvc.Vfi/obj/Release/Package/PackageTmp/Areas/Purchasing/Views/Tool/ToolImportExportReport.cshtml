@using Vfi.Ui.Mvc.Vfi.Areas.Inv.Models
@using Vfi.Ui.Mvc.Vfi.Areas.Purchasing.Models
@model MaterialInventoryModel
@{
    ViewBag.Title = "Công cụ xuất nhập";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@section Scripts{
    <!-- $.() -->


    <!-- binding -->
    <script type="text/javascript">

        var startGrid = false;

        function ToolType_OnChange() {
            RefreshGrid("gridInventory");
        }

        function gridInventory_OnDataBinding(args) {

            if (startGrid) {
                OnLoadingData();
                var vendor = $("#Vendor").data("tComboBox").value();
                if (vendor === "") {
                    vendor = 0;
                }
                var reportType = $("#ReportType").data("tDropDownList").value();
                if (reportType === null)
                    reportType = 2;
                var toolType = $("#ToolType").data('tComboBox').value();
                if (toolType === "")
                    toolType = 0;

                //var materialName = $("#MaterialCodeName").data("tAutoComplete").value();
                var fromDate = $("#fromDate").data("tDatePicker").value();
                if (fromDate === null)
                    fromDate = "";
                else {
                    fromDate = $.telerik.formatString('{0:dd/MM/yyyy}', fromDate);
                }
                var toDate = $("#toDate").data("tDatePicker").value();
                if (toDate === null)
                    toDate = "";
                else {
                    toDate = $.telerik.formatString('{0:dd/MM/yyyy}', toDate);
                }
                var toolName = $("#ToolName").data('tAutoComplete').value();
                var type = $("#Type").data('tDropDownList').value();
                if (type === "")
                    type = 0;
                var identity = $("#Identity").data('tComboBox').value();
                args.data = $.extend(args.data,
                {
                    vendorId: vendor,
                    toolName: toolName,
                    toolTypeId: toolType,
                    type: type,
                    identity: identity,
                    reportType: reportType,
                    fromDate: fromDate,
                    toDate: toDate
                });
            }
        }

        function grid_OnDataBound() {
            OnLoadedData();
        }

        var onLoading = false;
        function OnLoadingData() {
            if (!onLoading) {
                onLoading = true;
                var loadings = document.querySelectorAll('.twoToneButton');
                for (var i = 0; i < loadings.length; i++) {
                    var loading = loadings[i];
                    loading.innerHTML = "<span class='t-icon t-refresh t-loading'></span> (Chờ load)";
                    loading.classList.add('loading');
                }
            }
        }
        function OnLoadedData() {
            if (onLoading) {
                var loadings = document.querySelectorAll('.twoToneButton');
                for (var i = 0; i < loadings.length; i++) {
                    var loading = loadings[i];
                    if (loading && loading.className.includes("loading")) {
                        loading.classList.remove('loading');
                        loading.innerHTML = "<span></span>";
                    }
                }
                onLoading = false;
            }
        }

        function Type_OnLoad() {
            var combobox = $('#Type').data('tDropDownList');
            var dataSource =
            [
                { Text: "Tất cả", Value: "0" },
                { Text: "KHO A", Value: "1" },
                { Text: "KHO B", Value: "2" }
            ];

            combobox.dataBind(dataSource);
            combobox.select(0);
        }

        function RefreshGrid2(name) {
                OnLoadingData();
                startGrid = true;
                RefreshGrid(name);
            }

    </script>

    <!-- report -->
    <script type="text/javascript">

        function printMaterialInvTotalInMonth() {
            if (!onLoading) {
                OnLoadingData();
                var fromDate = $("#fromDate").data("tDatePicker").value();
                if (fromDate === null)
                    fromDate = "";
                else {
                    fromDate = $.telerik.formatString('{0:dd/MM/yyyy}', fromDate);
                }
                var toDate = $("#toDate").data("tDatePicker").value();
                if (toDate === null)
                    toDate = "";
                else {
                    toDate = $.telerik.formatString('{0:dd/MM/yyyy}', toDate);
                }
                var type = $('#Type').data('tDropDownList').value();
                if (type === "")
                    type = 0;
                $('#result').load('@Url.Action("PrintToolInvTotalInMonth", "Tool")',
                    {
                        type: type,
                        fromDate: fromDate,
                        toDate: toDate
                    },
                    function () {
                        OnLoadedData();
                        var window = $('#windowReport').data('tWindow');
                        window.open();
                    });
            }
            return false;
        }

        function printAssignProductionTool() {
            if (!onLoading) {
                OnLoadingData();
                $('#result').load('@Url.Action("PrintAssignProductionTool", "Tool")',
                    {},
                    function () {
                        OnLoadedData();
                        var window = $('#windowReport').data('tWindow');
                        window.open();
                    });
            }
            return false;
        }

        function printProductionTool() {
            if (!onLoading) {
                OnLoadingData();
                $('#result').load('@Url.Action("PrintProductionTool", "Tool")',
                    {},
                    function () {
                        OnLoadedData();
                        var window = $('#windowReport').data('tWindow');
                        window.open();
                    });
            }
            return false;
        }


        function printProductionToolByDay() {
            if (!onLoading) {
                OnLoadingData();
                var monthlyDate = $("#MonthlyDate").data("tDatePicker").value();
                if (monthlyDate === null)
                    monthlyDate = "";
                else
                    monthlyDate = $.telerik.formatString('{0:dd/MM/yyyy}', monthlyDate);

                $('#result').load('@Url.Action("PrintProductionToolByDay", "Tool")',
                    { monthlyDate: monthlyDate },
                    function () {
                        OnLoadedData();
                        var window = $('#windowReport').data('tWindow');
                        window.open();
                    });
            }
            return false;



        }

        function printToolInvInMonth() {
            if (!onLoading) {
                OnLoadingData();
                var vendor = $("#Vendor").data("tComboBox").value();
                if (vendor === "") {
                    vendor = 0;
                }
                var fromDate = $("#fromDate").data("tDatePicker").value();
                if (fromDate === null)
                    fromDate = "";
                else {
                    fromDate = $.telerik.formatString('{0:dd/MM/yyyy}', fromDate);
                }
                var toDate = $("#toDate").data("tDatePicker").value();
                if (toDate === null)
                    toDate = "";
                else {
                    toDate = $.telerik.formatString('{0:dd/MM/yyyy 23:59:59}', toDate);
                }
                var type = $('#Type').data('tDropDownList').value();
                if (type === "")
                    type = 0;
                var identity = $('#Identity').data('tComboBox').value();
                var toolType = $("#ToolType").data('tComboBox').value();
                if (toolType === "")
                    toolType = 0;
                $('#result').load('@Url.Action("PrintToolInvTotalInMonth", "Tool")',
                    {
                        vendorId: vendor,
                        fromDate: fromDate,
                        toDate: toDate,
                        type: type,
                        toolTypeId: toolType,
                        identity: identity,
                    }, function () {
                        OnLoadedData();
                        var window = $('#windowReport').data('tWindow');
                        window.open();
                    });
            }
            return false;
        }

        function reportType_onLoad() {
            var combobox = $('#ReportType').data('tDropDownList');
            var dataSource =
            [
                { Text: "Tất cả", Value: "1" },
                { Text: "Diễn biến", Value: "2" }
            ];

            combobox.dataBind(dataSource);
            combobox.select(1);
        }

    </script>

}

<div class="title">Báo cáo tổng hợp công cụ</div>

<div class="t-info-sha" style="padding-left: 15px; padding-right: 15px;">
    
    <div class="grid_7">
        Phân loại công cụ
            <ul>
                <li>A - Công cụ cắt gọt</li>
                <li>B - Chi tiết máy</li>
                <li>C - Dụng cụ đo</li>
                <li>D - Thiết bị điện</li>
            </ul>
    </div>
    <div class="grid_10">
        <button id="btnAssignTool" class="t-button" onclick=" return printAssignProductionTool(); ">
            <span class='t-icon t-insert'></span>
            In phát công cụ sản xuất 
            <span class="twoToneButton"></span>
        </button>
        <div class="clear"></div>
        <button id="btnProductionTool" class="t-button" onclick=" return printProductionTool(); ">
            <span class='t-icon t-insert'></span>
            Định mức công cụ sản xuất hiện tại
            <span class="twoToneButton"></span>
        </button>
    </div>


     <div class="grid_7">
        <button id="btnAssignTool1" class="t-button" onclick=" return printProductionToolByDay(); ">
            <span class='t-icon t-insert'></span>
            Định mức công cụ sản xuất theo ngày
            <span class="twoToneButton"></span>
        </button>
      </div>

            <div class="grid_3">
                @{
                    Html.Telerik().DatePicker()
                        .Name("MonthlyDate")
                        .Format("dd/MM/yyyy")
                        .Value(DateTime.Today)
                        .Render();
                }
        </div>


    <div class="clear"></div>
    <br />
    <fieldset>
        <legend>Tiêu chí trích lọc</legend>
        <div class="grid_1">NCC:</div>
        <div class="grid_5">
            @{
                Html.Telerik().ComboBox()
                    .Name("Vendor")
                    .DataBinding(binding => binding.Ajax().Select("SelectComboBoxToolVendor", "Vendor", new { area = "Purchasing" }))
                    .HtmlAttributes(new { style = "width: 100%" })
                    .AutoFill(true)
                    .Filterable(f=>f.FilterMode(Telerik.Web.Mvc.UI.AutoCompleteFilterMode.Contains))
                    .Render();
            }
        </div>
        <div class="grid_7">
            <div class="grid_2_5">Loại công cụ</div>
            <div class="grid_4">
                @{
                    Html.Telerik().ComboBox()
                        .Name("ToolType")
                        .DataBinding(binding => binding.Ajax().Select("SelectComboBoxToolType", "Tool", new { area = "Purchasing" }))
                        .HtmlAttributes(new { style = "width: 100%" })
                        //.DropDownHtmlAttributes(new { style = "width: 100%" })
                        .AutoFill(true)
                        .Filterable(f => f.FilterMode(Telerik.Web.Mvc.UI.AutoCompleteFilterMode.Contains))
                        .ClientEvents(e => e.OnChange("ToolType_OnChange"))
                        .Render();
                }
            </div>
            <div class="grid_2_5">Tên công cụ</div>
            <div class="grid_4">
                @{
                    Html.Telerik().AutoComplete()
                        .Name("ToolName")
                        .HtmlAttributes(new { style = "width: 100%" })
                        .Render();
                }
            </div>
        </div>
        <div class="grid_3">
            @{
                Html.Telerik().DropDownList()
                    .Name("Type")
                    .HtmlAttributes(new { style = "width: 100%" })
                    .ClientEvents(events => events.OnLoad("Type_OnLoad"))
                    .Render();
            }
            @{
                Html.Telerik().ComboBox()
                    .Name("Identity")
                    .DataBinding(binding => binding.Ajax().Select("SelectComboBoxToolIdentity", "Tool", new { area = "Purchasing" }))
                    .HtmlAttributes(new { style = "width: 100%" })
                    .Render();
            }
        </div>
        <div class="grid_3">
            @{
                Html.Telerik().DropDownList()
                    .Name("ReportType")
                    .HtmlAttributes(new { style = "width: 100%" })
                    .ClientEvents(events => events.OnLoad("reportType_onLoad"))
                    .Render();
            }
        </div>
        <div class="grid_3">
            @{
                Html.Telerik().DatePicker()
                    .Name("fromDate")
                    .Format("dd/MM/yyyy")
                    .Value(new DateTime(DateTime.Now.Year, DateTime.Now.Month, 1))
                    .Render();
            }
            @{
                Html.Telerik().DatePicker()
                    .Name("toDate")
                    .Format("dd/MM/yyyy")
                    .Value(new DateTime(DateTime.Now.Year, DateTime.Now.Month, 1).AddMonths(1).AddSeconds(-1))
                    .Render();
            }
        </div>
        <div class="grid_3">
        </div>
        <div class="grid_3">
            <button id="btnRefesh" class="t-button" onclick=" return RefreshGrid2('gridInventory'); ">
                <span class='t-icon t-insert'></span>
                Làm mới
                <span class="twoToneButton"></span>
            </button>
        </div>
        <div class="grid_5">
            <button id="btnMaterialInv" class="t-button" onclick=" return printToolInvInMonth(); ">
                <span class='t-icon t-insert'></span>
                Xuất báo cáo tồn kho
                <span class="twoToneButton"></span>
            </button>
        </div>
    </fieldset>

</div>
<div class="clear"></div>
<div class="cover-div">
    @{
        Html.Telerik().Grid<ToolInventoryModel>()
            .Name("gridInventory")
            .DataKeys(keys => keys.Add(f => f.ToolInvId).RouteKey("toolInvId"))
            .DataBinding(dataBindings => dataBindings.Ajax()
                .Select("SelectToolInvTotal", "Tool",
                    new {
                        vendorId = 0,
                        toolTypeId = -1, toolName = "",
                        type = 0, idenitty = "",
                        reportType = 0,
                        fromDate = "", toDate = ""
                    })
                .Update("UpdateToolInvTotal", "Tool")
            )

            .Columns(columns => {
                columns.Command(c => c.Edit().ButtonType(Telerik.Web.Mvc.UI.GridButtonType.Image)).Width(50).Title("");
                columns.Bound(f => f.MaterialTypeName).Width(75).ReadOnly(true)
                    .HeaderTemplate("Loại");
                columns.Bound(f => f.ToolName).Width(120).Title("Tên công cụ").ReadOnly(true)
                    .HtmlAttributes(new { style = "font-weight:bold;" });
                columns.Bound(f => f.ToolCode).Width(100).Title("Mã công cụ").ReadOnly(true)
                    .HtmlAttributes(new { style = "font-weight:bold;" })
                    .Aggregate(agg => agg.Count())
                    .ClientGroupFooterTemplate("<#= Count #>")
                    .ClientFooterTemplate("<#= Count #>");
                columns.Bound(f => f.ToolFullCode).Width(150).Title("Công cụ").ReadOnly(true)
                    .HtmlAttributes(new { style = "font-weight:bold;" });
                //columns.Bound(f => f.ToolCode).Width(100).Title("Mã tên")
                //    .HtmlAttributes(new { style = "font-weight:bold;" });
                //columns.Bound(f => f.ToolDesign).Width(150).Title("Quy cách")
                //    .HtmlAttributes(new { style = "font-weight:bold;" });
                //columns.Bound(f => f.ToolMaterial).Width(75).Title("Nguyên liệu")
                //    .HtmlAttributes(new { style = "font-weight:bold;" });
                //columns.Bound(f => f.ToolProduction).Width(75).Title("Nguyên liệu")
                //    .HtmlAttributes(new { style = "font-weight:bold;" });
                columns.Bound(f => f.VendorCode).Width(55).ReadOnly(true)
                    .HeaderTemplate("NCC");
                columns.Bound(f => f.LotNumber).Title("Lô").Width(70).ReadOnly(true);
                columns.Bound(f => f.UnitMeasure).Title("ĐVT").Width(50).ReadOnly(true);
                columns.Bound(f => f.UnitPrice).Title("Đơn giá").Width(80).ReadOnly(true)
                    .Format("{0:n0}")
                    .HtmlAttributes(new { style = "text-align: right;" });
                columns.Bound(f => f.Early).Width(70).ReadOnly(true)
                    .HeaderTemplate("<strong>Tồn<br/>đầu</strong>")
                    .HtmlAttributes(new { style = "text-align: right;" })
                    .Format("{0:n0}")
                    .Aggregate(agg => agg.Sum())
                    .ClientGroupFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                    .ClientFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                    .FooterHtmlAttributes(new { style = "text-align: right;border-left:1px black solid;" });
                columns.Bound(f => f.EarlyPrice).Width(90).ReadOnly(true)
                    .HeaderTemplate("<strong>Giá đầu</strong>")
                    .HtmlAttributes(new { style = "text-align: right;border-right:1px black solid;" })
                    .Format("{0:n0}")
                    .Aggregate(agg => agg.Sum())
                    .ClientGroupFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                    .ClientFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                    .FooterHtmlAttributes(new { style = "text-align: right;border-right:1px black solid;" });
                //
                columns.Bound(f => f.Import).Width(60).ReadOnly(true)
                    .HeaderTemplate("<strong>Nhập<br/>mua</strong>")
                    .HtmlAttributes(new { style = "text-align: right;" })
                    .Format("{0:n0}")
                    .Aggregate(agg => agg.Sum())
                    .ClientGroupFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                    .ClientFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                    .FooterHtmlAttributes(new { style = "text-align: right;" });
                columns.Bound(f => f.ImportMore).Width(60).ReadOnly(true)
                    .HeaderTemplate("<strong>Nhập<br/>thêm</strong>")
                    .HtmlAttributes(new { style = "text-align: right;" })
                    .Format("{0:n0}")
                    .Aggregate(agg => agg.Sum())
                    .ClientGroupFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                    .ClientFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                    .FooterHtmlAttributes(new { style = "text-align: right;" });
                columns.Bound(f => f.ImportInternal).Width(60).ReadOnly(true)
                    .HeaderTemplate("<strong>Nhập<br/>NB</strong>")
                    .HtmlAttributes(new { style = "text-align: right;" })
                    .Format("{0:n0}")
                    .Aggregate(agg => agg.Sum())
                    .ClientGroupFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                    .ClientFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                    .FooterHtmlAttributes(new { style = "text-align: right;" });
                columns.Bound(f => f.Export).Width(60).ReadOnly(true)
                    .HeaderTemplate("<strong>Sử<br/>dụng</strong>")
                    .HtmlAttributes(new { style = "text-align: right;border-left:1px black solid;" })
                    .Format("{0:n0}")
                    .Aggregate(agg => agg.Sum())
                    .ClientGroupFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                    .ClientFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                    .FooterHtmlAttributes(new { style = "text-align: right;border-left:1px black solid;" });
                columns.Bound(f => f.ExportDestroy).Width(60).ReadOnly(true)
                    .HeaderTemplate("<strong>Xuất<br/>hủy</strong>")
                    .HtmlAttributes(new { style = "text-align: right;" })
                    .Format("{0:n0}")
                    .Aggregate(agg => agg.Sum())
                    .ClientGroupFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                    .ClientFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                    .FooterHtmlAttributes(new { style = "text-align: right;" });
                columns.Bound(f => f.ExportInternal).Width(60).ReadOnly(true)
                    .HeaderTemplate("<strong>Xuất<br/>NB</strong>")
                    .HtmlAttributes(new { style = "text-align: right;" })
                    .Format("{0:n0}")
                    .Aggregate(agg => agg.Sum())
                    .ClientGroupFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                    .ClientFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                    .FooterHtmlAttributes(new { style = "text-align: right;" });
                columns.Bound(f => f.Last).Width(70).ReadOnly(true)
                    .HeaderTemplate("<strong>Tồn<br/>cuối</strong>")
                    .HtmlAttributes(new { style = "text-align: right;border-left:1px black solid;" })
                    .Format("{0:n0}")
                    .Aggregate(agg => agg.Sum())
                    .ClientGroupFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                    .ClientFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                    .FooterHtmlAttributes(new { style = "text-align: right;border-left:1px black solid;" });
                columns.Bound(f => f.LastPrice).Width(90).ReadOnly(true)
                    .HeaderTemplate("<strong>Giá cuối</strong>")
                    .HtmlAttributes(new { style = "text-align: right;border-right:1px black solid;" })
                    .Format("{0:n0}")
                    .Aggregate(agg => agg.Sum())
                    .ClientGroupFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                    .ClientFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                    .FooterHtmlAttributes(new { style = "text-align: right;border-right:1px black solid;" });
                columns.Bound(f => f.StoreCode).Width(150)
                    .Title("Mã Kho");
                columns.Bound(f => f.ImportDateString).Width(90).ReadOnly(true)
                    .Title("Ngày nhập");
                columns.Bound(f => f.FirstImportQuantity).Width(50).ReadOnly(true)
                    .HeaderTemplate("SL</br>nhập")
                    .HtmlAttributes(new { style = "text-align: right;" });
                columns.Bound(f => f.FirstUseDateString).Width(90).ReadOnly(true)
                    .HeaderTemplate("Ngày BĐ</br>sử dụng");
                columns.Bound(f => f.FirstUseDateString).Width(90).ReadOnly(true)
                    .HeaderTemplate("Ngày cuối</br>sử dụng");
                columns.Bound(f => f.Export3Month).Width(60).ReadOnly(true)
                    .HeaderTemplate("Xuất</br>3 T");
                columns.Bound(f => f.ProductCodes).Width(660).ReadOnly(true)
                    .HeaderTemplate("Sản phẩm</br>sử dụng");

            })
            .DetailView(details => details.ClientTemplate(
                Html.Telerik().TabStrip()
                    .Name("TabStrip_<#= ToolInvId #>")
                    .SelectedIndex(0)
                    .Items(items =>
                        items.Add().Text("Thông tin chi tiết").Content(
                            Html.Telerik().Grid<TransactionFptDetailModel>()
                                .Name("gridDetail_<#= ToolInvId #>")
                                .DataBinding(dataDindings => dataDindings.Ajax()
                                    .Select("SelectToolInvDetailTotal", "Tool",
                                        new {
                                            toolInvId = "<#= ToolInvId #>",
                                            fromDate = "<#= FromDate #>",
                                            toDate = "<#= ToDate #>",
                                        }))
                                .Columns(columns => {
                                    columns.Bound(f => f.TransactionDate).Title("Ngày").Width(100)
                                        .Format("{0:dd/MM/yyyy}");
                                    columns.Bound(f => f.EoIName).Title("Loại").Width(100);
                                    columns.Bound(f => f.Quantity).Width(75).Title("SL")
                                        .Format("{0:n2}")
                                        .HtmlAttributes(new { style = "text-align: right;" });
                                    columns.Bound(f => f.ModifiedUser).Width(100).Title("Người làm");
                                    columns.Bound(f => f.ModifiedDate).Title("Ngày làm").Width(100)
                                        .Format("{0:dd/MM/yyyy HH:mm}");
                                    columns.Bound(f => f.Note).Title("Ghi chú");
                                })
                                .ClientEvents(events => events.OnError("grid_OnError"))
                                .ToHtmlString()
                            )
                    )
                    .ToHtmlString()
                ))
            .Scrollable(f => f.Height(510))
            //.HtmlAttributes(new { style = "width: 950px;" })
            .Resizable(f => f.Columns(true))
            .Reorderable(f => f.Columns(true))
            .Filterable()
            .Sortable()
            .Groupable()
            .ClientEvents(events => events.OnError("grid_OnError")
                .OnDataBinding("gridInventory_OnDataBinding")
                .OnDataBound("grid_OnDataBound"))
            .Render();
    }
</div>

<!-- windown -->
@{
    Html.Telerik().Window()
        .Name("windowReport")
        .Modal(true)
        .Draggable(true)
        .Content
        (
    @<text>
    <div id="result"></div>
    </text>
)
        //.ClientEvents(e => e.OnLoad("Window_onLoad"));
        .Visible(false)
        .Width(960)
        .Render();
}

<style type="text/css">
    #windowReport {
        /*
            float: right;
            Position: relative;
            */
        min-height: 580px;
        top: 15%;
    }

    #result {
        height: 520px;
        overflow: hidden;
    }

    #ReportBody {
        padding-bottom: 25px;
        margin-bottom: 25px;
    }

    .tb-witdh td {
        width: 55px;
    }
</style>

