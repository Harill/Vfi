@using Vfi.Ui.Mvc.Vfi.Areas.Purchasing.Models
@using Vfi.Ui.Mvc.Vfi.Utilities
@model PurchaseOrderModel
@{
    ViewBag.Title = "Duyệt yêu cầu";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@section Scripts{
    <script type="text/javascript" src="@Url.Content("~/Scripts/OnProcess.js")"></script>
    <script type="text/javascript">

        function materialClassified_OnChange() {
            var classified = $('#MaterialClassifiedName').data('tDropDownList').value();
            var vendor = $('#VendorCodeName').data('tComboBox');
            //RefreshCombobox('VendorCodeName');
            //RefreshDropDownList('VendorCodeName');
            //RefreshGrid("grid");
            $.ajax({
                type: 'post',
                data: { classified: classified },
                dataType: 'json',
                url: '@Url.Action("SelectComboBoxVendorByClassified", "PurchaseOrder", new { area = "Purchasing" })',
                timeout: 20000,
                success: function (data) {
                    vendor.dataBind(data);
                    RefreshGrid("grid");
                }
            });
        }
        var classifiedId = 0;
        function grid_OnEdit(e) {
            classifiedId = e.dataItem.ClassifiedId;
        }

        function grid_OnDataBinding(args) {
            var statusValue = $("#Status").data("tComboBox").value();
            if (statusValue === "")
                statusValue = 0;

            var classified = $("#MaterialClassifiedName").data("tDropDownList").value();
            if (classified === "")
                classified = 0;
            var vendor = $("#VendorCodeName").data("tComboBox").value();
            if (vendor === "")
                vendor = 0;

            //var fromDate = $("#fromDate").data("tDatePicker").value();
            //if (fromDate === null)
            //    fromDate = "";
            //else {
            //    fromDate = $.telerik.formatString('{0:dd/MM/yyyy}', fromDate);
            //}
            //var toDate = $("#toDate").data("tDatePicker").value();
            //if (toDate === null)
            //    toDate = "";
            //else {
            //    toDate = $.telerik.formatString('{0:dd/MM/yyyy 23:59:59}', toDate);
            //}
            args.data = $.extend({}, args.data, {
                classified: classified,
                status: statusValue,
                vendorId: vendor
            });
        }


        function grid_OnRowDataBound(e) {
            var dataItem = e.dataItem;
            var $row = $(e.row);

            // màu trạng thái
            if (dataItem.State == 1) {
                $row.children().eq(7).css("background-color", "#FA5858");
            } else if (dataItem.State == 2) {
                $row.children().eq(7).css("background-color", "yellow");
            }

            // xử lý nút ký
            var $btn = $row.find(".t-button.purchasing");
            var $chk = $row.find(".chkCheck");

            if (dataItem.PurchasingSignatureType == 0) {
                $btn.parent().html("<span class='t-icon t-cancel'></span>");
                $chk.remove();
            }
            else if (dataItem.PurchasingSignatureType == 1) {
                $btn.parent().html("<span class='t-icon t-insert'></span>");
            }
            else if (dataItem.PurchasingSignatureType == 2) {
                $chk.remove();
            }
            else {
                $btn.parent().html("");
            }
        }




        //function grid_OnRowDataBound(e) {
        //    if (e.dataItem.State == 1) {
        //        e.row.childNodes[7].bgColor = "#FA5858";
        //    }
        //    else if (e.dataItem.State == 2) {
        //        e.row.childNodes[7].bgColor = "yellow";
        //    }
        //    var dataItem = e.dataItem;
        //    if (dataItem.PurchasingSignatureType == 0) {
        //        document.getElementsByClassName("t-button purchasing")[indexSignature1 - countSignature1].parentNode.innerHTML = "<span class='t-icon t-cancel'></span>";
        //        document.getElementsByClassName("chkCheck")[index1 - count1].parentNode.innerHTML = "";
        //        countSignature1++;
        //        count1++;
        //    } else if (dataItem.PurchasingSignatureType == 1) {
        //        document.getElementsByClassName("t-button purchasing")[indexSignature1 - countSignature1].parentNode.innerHTML = "<span class='t-icon t-insert'></span>";
        //        countSignature1++;
        //    } else if (dataItem.PurchasingSignatureType == 2) {
        //        document.getElementsByClassName("chkCheck")[index1 - count1].parentNode.innerHTML = "";
        //        count1++;
        //    } else {
        //        document.getElementsByClassName("t-button purchasing")[indexSignature1 - countSignature1].parentNode.innerHTML = "";
        //        countSignature1++;

        //    }
        //    indexSignature1++;
        //    index1++;
        //}

        function vendor_OnChange() {
            RefreshGrid("grid");
        }
        function status_OnChange() {
            RefreshGrid("grid");
        }

        function vendorGrid_OnDataBinding(e) {
            e.data = $.extend(e.data, {
                classified: classifiedId,
            });
        }
    </script>


    <script type="text/javascript">
        function printInquiryForm() {
            
            if (!onProcessing) {
                OnProcessingData();
                var $checkedRecords = $("input[name='chkIds']:checked")

                if ($checkedRecords.length < 1) {
                    alert("Vui lòng chọn ít nhất 1 sản phẩm");
                    return;
                }

                var a = {};
                var ids = "";

                for (var i = 0; i < $checkedRecords.length; i++) {
                    if ($checkedRecords[i] === "0") continue;
                    a[i] = $checkedRecords[i].value;

                    ids += $checkedRecords[i].value;
                    if (i < ($checkedRecords.length - 1))
                        ids += ":";
                }
                //alert(ids);
                $('#result').load('@Url.Action("PrintInquiryForm", "PurchaseOrder", new { area = "Purchasing" })',
                    {
                        ids: ids,
                    }, function () {
                        OnProcessedData();
                        var window = $('#windowReport').data('tWindow');
                        window.open();
                    });
            }
            return false;
        }

        function transformToPO() {
            if (!onProcessing) {
                OnProcessingData();
                var $checkedRecords = $("input[name='chkIds']:checked")

                if ($checkedRecords.length < 1) {
                    alert("Vui lòng chọn ít nhất 1 sản phẩm");
                    return;
                }

                var a = {};
                var ids = "";

                for (var i = 0; i < $checkedRecords.length; i++) {
                    if ($checkedRecords[i] === "0") continue;
                    a[i] = $checkedRecords[i].value;

                    ids += $checkedRecords[i].value;
                    if (i < ($checkedRecords.length - 1))
                        ids += ":";
                }
                $.ajax({
                    type: 'post',
                    data: { ids: ids },
                    dataType: 'json',
                    url: '@Url.Action("TransformToPo", "PurchaseOrder", new { area = "Purchasing" })',
                    timeout: 20000,
                    success: function (data) {
                        alert(data);
                        RefreshGrid("grid");
                        OnProcessedData();
                    }
                });
            }
        }


        function Signature(inquiryId) {
            $.ajax({
                type: 'post',
                data: { inquiryId: inquiryId },
                dataType: 'json',
                url: '@Url.Action("ImportSignature", "Transaction", new { area = "Inv" })',
                timeout: 20000,
                success: function (data) {
                    if (data == 9)
                        alert("Lỗi không tìm thấy phiếu !");
                    else {
                        alert("Xác nhận thành công !");
                        RefreshGrid("grid");
                        indexSignature1 = 0;
                        countSignature1 = 0;
                        index1 = 0;
                        count1 = 0;
                    }
                }
            });
            return false;
        }
    </script>


}


<div class="title">Duyệt phiếu yêu cầu</div>
<div class="t-info-sha" style="padding-left: 15px; padding-right: 15px;">

    <fieldset>
        <legend>Thông tin phiếu yêu cầu</legend>

        <div class="grid_5">@Html.LabelFor(f => f.MaterialClasstifiedName)</div>
        <div class="grid_6">@Html.LabelFor(f => f.VendorCodeName)
           @* <button class="t-button" id="btnTypeRefresh" name="btnPickupMaterialId" onclick=" TypeRefresh() ">
                <span class='t-icon t-refresh'></span>
            </button>*@
        </div>

        @*<div class="grid_5">@Html.LabelFor(f => f.CurrencyCode)</div>*@

        <div class="clear"></div>
        <div class="grid_5">
            @{
                Html.Telerik().DropDownList()
                    .Name("MaterialClassifiedName")
                    .DataBinding(binding => binding.Ajax().Select("SelectComboBoxMaterialClassified", "Material", new { area = "" }))
                    .HtmlAttributes(new { style = "width: 175px" })
                    .DropDownHtmlAttributes(new { style = "width: 250px" })
                    .ClientEvents(e => e.OnChange("materialClassified_OnChange"))
                    .Render();                                              
            }
        </div>
        <div class="grid_6">
            @{
                Html.Telerik().ComboBox()
                    .Name("VendorCodeName")
                    //.DataBinding(binding => binding.Ajax().Select("SelectComboBoxVendorByClassified", "PurchaseOrder"))
                    .HtmlAttributes(new { style = "width: 175px" })
                    .DropDownHtmlAttributes(new { style = "width: 250px" })
                    .ClientEvents(e => e.OnChange("vendor_OnChange"))
                    .Render();                                              
            }
        </div>
       @* <div class="grid_5">
            @{
                Html.Telerik().DropDownList()
                    .Name("CurrencyCode")
                    .DataBinding(binding => binding.Ajax().Select("SelectComboBoxCurrency", "Currency", new { area = "" }))
                    .DropDownHtmlAttributes(new { style = "width: 250px" })
                    .Render();
            }
        </div>*@
        
       <div class="grid_2">
            Tình trạng
        </div>
        <div class="grid_4">
            @{
                Html.Telerik().ComboBox()
                    .Name("Status")
                    .DataBinding(binding => binding.Ajax().Select("SelectComboBoxInquiryStatus", "PurchaseOrder", new { area = "Purchasing" }))
                    .HtmlAttributes(new { style = "width: 150px" })
                    .DropDownHtmlAttributes(new { style = "width: 250px" })
                    .ClientEvents(e => e.OnChange("status_OnChange"))
                    .Filterable(f => f.FilterMode(Telerik.Web.Mvc.UI.AutoCompleteFilterMode.Contains))
                    .HighlightFirstMatch(true)
                    .AutoFill(true)
                    .Render();
            }
        </div>
        <div class="grid_1">
            <button class="t-button" id="btnGridRefresh" name="btnPickupMaterialId" onclick=" RefreshGrid('grid') ">
                <span class='t-icon t-refresh'>Làm mới</span>
            </button>
        </div>
        <div class="grid_6">
            <button class="t-button" id="btnPrintForm" name="btnPickupId" onclick=" printInquiryForm() ">
                <span class='t-icon t-insert'></span>
                In Phiếu Yêu Cầu
                <span class="processButton"></span>
            </button>

        </div>
    </fieldset>
</div>
<div class="clear"></div>


<div class="cover-div">
    @{
        Html.Telerik().Grid<InquiryPoModel>()
            .Name("grid")
            .DataKeys(keys => keys.Add(f => f.InquiryId).RouteKey("inquiryId"))
            
            .ToolBar(bar => bar.Template(
        @<text>
        <div class="grid_9">
            <button class="t-button" id="btnTransform" name="btnPickupId" onclick=" transformToPO() ">
                <span class='t-icon t-insert'></span>
                Chuyển thành mua hàng
                <span class="processButton"></span>
            </button>
        </div>
        <div class="clear"></div>
        </text>
))
            .DataBinding(dataBindings => dataBindings.Ajax()
                .Select("SelectInquiryPo", "PurchaseOrder", new {
                    classified = 0,
                    type = 0,
                    vendorId = 0,
                    status = 0,
                    fromDate = "",
                    toDate = ""
                })
                .Update("UpdateInquiryPo", "PurchaseOrder", new {
                    classified = 0,
                    type = 0,
                    vendorId = 0,
                    status = 0,
                })
                .Delete("CancelInquiryPo", "PurchaseOrder", new {
                    classified = 0,
                    type = 0,
                    vendorId = 0,
                    status = 0,
                })
            )
            .Columns(columns => {
                columns.Bound(f => f.InquiryId).Title("Chọn").Width(30).ReadOnly(true)
                    .ClientTemplate("<# if (PurchasingSignatureType == 1) { #>" + "<input type='checkbox' name='chkIds' class='chkCheck' value='<#= InquiryId #>' />" + "<# } #>")
                    .HtmlAttributes(new { style = "text-align:center; border-bottom:1px black solid; " });
                columns.Command(command => command.Edit().ButtonType(Telerik.Web.Mvc.UI.GridButtonType.Image))
                    .Width(40).Title("Sửa")

                    .HtmlAttributes(new { style = "text-align:center; border-bottom:1px black solid; " });

                //columns.Bound(f => f.ReferenceId).Width(90).Title("Số ID").ReadOnly(true);

                columns.Bound(f => f.InquiryNumber).Width(80).Title("Số yêu cầu").ReadOnly(true)
                    .HtmlAttributes(new { style = "border-bottom:1px black solid; " })
                    .ClientGroupFooterTemplate("<#= $.telerik.formatString('{0:n0}', Count) #>")
                    .ClientFooterTemplate("<#= $.telerik.formatString('{0:n0}', Count) #>")
                    .FooterHtmlAttributes(new { style = "text-align: center; font-weight:bold; " })
                    .Aggregate(agg => agg.Count());
                columns.Bound(f => f.ClassifiedName).Width(100).Title("Phân loại").ReadOnly(true)
                    .HtmlAttributes(new { style = "border-bottom:1px black solid; " });
                columns.Bound(f => f.TypeName).Width(90).Title("Loại").ReadOnly(true)
                    .HtmlAttributes(new { style = "border-bottom:1px black solid; " });
                columns.Bound(f => f.ReferenceName).Width(140).Title("Tên").ReadOnly(true)
                    .HtmlAttributes(new { style = "border-bottom:1px black solid; " });
                columns.Bound(f => f.ReferenceCode).Width(190).Title("Mã").ReadOnly(true)
                    .HtmlAttributes(new { style = "border-bottom:1px black solid; font-weight:bold;" });
                columns.Bound(f => f.TotalInv).Width(60).Title("Tồn kho").ReadOnly(true)
                    .HtmlAttributes(new { style = "text-align: right; border-bottom:1px black solid; font-weight:bold;" })
                    .Format("{0:n2}")
                    .Aggregate(agg => agg.Sum())
                    .FooterHtmlAttributes(new { style = "text-align: right; font-weight:bold;" })
                    .ClientGroupFooterTemplate("<#= $.telerik.formatString('{0:n2}', Sum) #>")
                    .ClientFooterTemplate("<#= $.telerik.formatString('{0:n2}', Sum) #>"); ;
                columns.Bound(f => f.Total3MonthsUsed).Width(60).Title("SL SD</br> 3T").ReadOnly(true)
                    .HtmlAttributes(new { style = "text-align: right; border-bottom:1px black solid; border-right:2px black solid; font-weight:bold;" })
                    .Format("{0:n2}")
                    .Aggregate(agg => agg.Sum())
                    .FooterHtmlAttributes(new { style = "text-align: right; font-weight:bold;" })
                    .ClientGroupFooterTemplate("<#= $.telerik.formatString('{0:n2}', Sum) #>")
                    .ClientFooterTemplate("<#= $.telerik.formatString('{0:n2}', Sum) #>"); ;

                
                //
                columns.Bound(f => f.Unit).Width(50).Title("ĐVT")
                    .HtmlAttributes(new { style = "border-bottom:1px black solid; border-left:2px black solid;" });
                columns.Bound(f => f.VendorCode).Width(120).Title("NCC")
                    .HtmlAttributes(new { style = "border-bottom:1px black solid; " });
                columns.Bound(f => f.Currency).Width(40).Title("Tiền </br>tệ")
                    .HtmlAttributes(new { style = "border-bottom:1px black solid; " });
                columns.Bound(f => f.OrderQty).Width(50).Title("SL").ReadOnly(true)
                    .HtmlAttributes(new { style = "text-align: right; border-bottom:1px black solid; font-weight:bold;" })
                    .Format("{0:n0}")
                    .Aggregate(agg => agg.Sum())
                    .FooterHtmlAttributes(new { style = "text-align: right; font-weight:bold;" })
                    .ClientGroupFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                    .ClientFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>");
                columns.Bound(f => f.UnitPrice).Width(100).Title("ĐG")
                    .HtmlAttributes(new { style = "text-align: right; border-bottom:1px black solid;" })
                    .ClientTemplate("<#= UnitPriceStr #>");
                columns.Bound(f => f.Price).Width(100).Title("TT").ReadOnly(true)
                    .HtmlAttributes(new { style = "text-align: right; border-bottom:1px black solid; " })
                    .Format("{0:n0}")
                    .Aggregate(agg => agg.Sum())
                    .FooterHtmlAttributes(new { style = "text-align: right; " })
                    .ClientGroupFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>")
                    .ClientFooterTemplate("<#= $.telerik.formatString('{0:n0}', Sum) #>");
                columns.Bound(f => f.DueDate).Width(100).Title("Ngày YC")
                    .HtmlAttributes(new { style = "border-bottom:1px black solid; " })
                    .ClientTemplate("<#= DueDate == null ? '' : $.telerik.formatString('{0:dd/MM/yyyy}', DueDate) #>");
                columns.Bound(f => f.Note).Width(150)
                    .HtmlAttributes(new { style = "border-bottom:1px black solid; border-right:2px black solid; " });
                
                //
                columns.Bound(f => f.InquiryId).Title("Quản lý</br>xác nhận").Width(65)
                    .ClientTemplate("<button class='t-button purchasing' id='QLM_<#= InquiryId #>' onclick=Signature(<#= InquiryId #>) ><span class='t-icon t-insert'></span></button>")
                    .HtmlAttributes(new { style = "text-align:center; border-bottom:1px black solid; border-left:2px black solid;" });
                columns.Command(commands => commands.Delete().ButtonType(Telerik.Web.Mvc.UI.GridButtonType.Image)).Width(50).Title("")
                    .HtmlAttributes(new { style = "border-bottom:1px black solid; " });
                columns.Bound(f => f.ModifiedDate).Width(80).Title("Ngày làm").ReadOnly(true)
                    .HtmlAttributes(new { style = "border-bottom:1px black solid" })
                    .ClientTemplate("<#= ModifiedDate == null ? '' : $.telerik.formatString('{0:dd/MM/yyyy HH:mm}', ModifiedDate) #>");
                columns.Bound(f => f.ModifiedUser).Width(80).Title("Người làm").ReadOnly(true)
                    .HtmlAttributes(new { style = "border-bottom:1px black solid" });
            })
            .ClientEvents(events => events
                .OnDataBinding("grid_OnDataBinding")
                .OnRowDataBound("grid_OnRowDataBound")
                .OnEdit("grid_OnEdit")
                .OnError("grid_OnError")
            )
            .Groupable(group => group.Groups(g => g.Add(model => model.ClassifiedName)))
            .Scrollable(f => f.Height("auto"))
            .Sortable()
            .Filterable()
            .Resizable(resizing => resizing.Columns(true))
            .Footer(true)
            .Render();
    }
</div>
<!-- windown -->
@{
    Html.Telerik().Window()
        .Name("windowReport")
        .Modal(true)
        .Draggable(true)
        .Content
        (
    @<text>
    <div id="result"></div>
    </text>
)
        //.ClientEvents(e => e.OnLoad("Window_onLoad"));
        .Visible(false)
        .Width(960)
        .Render();
}
<style type="text/css">
    #windowReport {
        min-height: 580px;
        top: 15%;
    }

    #result {
        height: 520px;
        overflow: hidden;
    }

    #ReportBody {
        padding-bottom: 25px;
        margin-bottom: 25px;
    }

    .tb-witdh td {
        width: 55px;
    }
</style>

